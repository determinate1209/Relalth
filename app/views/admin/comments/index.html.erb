<h3>コメント一覧</h3>
<% @comments.each do |comment| %>

  <%= comment.customer.user_name %>
  <%= comment.comment %>
  <%= link_to "削除", menu_comment_path(comment.menu_id,comment.id), method: :delete %><br />
  
<% end %>
