<h3>メニュー一覧</h3>
<% @menus.each do |menu| %>
  <%= menu.id %>
  
  <%= link_to menu_path(menu.id) do %>
    <%= menu.name %>
  <% end %>
  
  <%= menu.description %>
  
  <%= link_to "編集する", edit_menu_path(menu.id) %>
  
<% end %>
