<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container">
    <a class="navbar-brand" href="/">
      <%= image_tag'logo.jpg',size:'170x60' %></a>
    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown" style="width:100%;">
      <div class="mx-auto"></div>
      <div class= "d-block">
        <ul class="navbar-nav">
          <li>
            <%= link_to new_hearing_path, class: 'nav-link mx-2 btn-text', style: "color:white;" do %>
              <i class="fas fa-stethoscope"></i>診断開始
            <% end %>
          </li>
          <li>
            <%= link_to menus_path, class: 'nav-link mx-2 btn-text', style:"color:white;" do %>
              <i class="fas fa-clipboard-list"></i>メニュー一覧
            <% end %>
          </li>
          <li>
            <%= link_to new_customer_registration_path, class: 'nav-link mx-2 btn-text', style: "color:white;" do %>
              <i class="fas fa-user-plus"></i>新規登録
            <% end %>
          </li>
          <li>
            <%= link_to new_customer_session_path, class: 'nav-link mx-2 btn-text', style: "color:white;" do %>
              <i class="fas fa-sign-in-alt"></i>ログイン
            <% end %>
          </li>
        </ul>
        <div class="form-inline d-block text-right">
          <%= form_with url: search_menus_path, local: true, method: :get do |f| %>
            <%= f.text_field :name,class: "page-text" %>
            <%= button_tag type: 'submit' do %>
              <i class="fas fa-search"></i>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>