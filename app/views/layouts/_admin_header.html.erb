<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container">
    <a class="navbar-brand" href="/">
      <%= image_tag'logo.jpg',size:'170x60' %></a>
    <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <div class="mx-auto"></div>
      <div class= "d-block">
        <ul class="navbar-nav ml-auto">
          <li>
            <%= link_to admin_customers_path, class: 'nav-link mx-2 btn-text', style: "color:white;" do %>
              <i class="fas fa-users"></i>会員一覧
            <% end %>
          </li>
          <li>
            <%= link_to admin_menus_path, class: 'nav-link mx-2 btn-text', style: "color:white;" do %>
              <i class="fas fa-clipboard-list"></i>メニュー一覧
            <% end %>
          </li>
          <li>
            <%= link_to destroy_admin_session_path, method: :delete, class: 'nav-link mx-2 btn-text', style: "color:white;" do %>
              <i class="fas fa-sign-out-alt"></i>ログアウト
            <% end %>
          </li>
        </ul>
        <div class="form-inline d-block text-right">  
          <%= form_with url: search_admin_menus_path, local: true, method: :get do |form| %>
            <%= form.text_field :name,class: "page-text" %>
            <%= button_tag type: 'submit' do %>
              <i class="fas fa-search"></i>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
  